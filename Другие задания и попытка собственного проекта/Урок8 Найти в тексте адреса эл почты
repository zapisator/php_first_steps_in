 <?php
	error_reporting(-1);
	
	$text_with_emails = 'Электронные адреса sosi_u_putina@kremlin.ru и privetmedved@kremlin.ru в числе прочих используются администраторами сайта президента России Владимира Путина, считает Евгений Медведев, консультант компании ТСС, специализирующейся на кибербезопасности/ Vедведев воспользовался сервисом Snovio, который позволяет посмотреть все адреса электронной почты, которые указаны где-либо как привязанные к нужному пользователю сайту. «Случайно зашел на сайт Кремля. Решил посмотреть: а тут что? А там sosi_u_putina@kremlin.ru! Все почты, которые я подчеркнул, это явно админов сайта работа. Они создали такие почты для своей коммуникации», — рассказал «Снобу» Медведев.';
	$regexp_for_split_into_words = '/ /';
	$regexp_emails = '/[\S]+[\S]+[.][a-zA-Z]{1,3}/u';
	$email_matches = array();
	preg_match_all($regexp_emails, $text_with_emails, $email_matches);
	$i = count ($email_matches);
	$j = count ($email_matches[0]);
	echo "Число строк = ".$i.".\n Число столбцов = ".$j."\n\n";
	for ($l = 0; $l <=$i-1; $l++){
		for ($k = 0; $k <= $j-1; $k++) {
			echo $email_matches[$l][$k]."\n";
		}
	}
	
	
	
?>